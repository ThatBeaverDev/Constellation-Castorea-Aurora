#! /System/apps/compilers/js


function compile(code) {
	return code;
};

async function init(args) {
	if (args[0] == undefined) {
		return;
	};

	const dir = await call.fullDirectory(args[0], parent.dir);

	await call.exec(dir, [], system !== undefined);
};